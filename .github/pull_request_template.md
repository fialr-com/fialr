## Summary

<!-- What changed and why. -->

## Checks

- [ ] `mkdocs build --strict` passes (no broken links/nav)
- [ ] `python scripts/check_docs_parity.py` passes (docs reference only real commands)
- [ ] `feature-manifest.json` re-synced from the private repo if the command surface changed
- [ ] No secrets; commits are signed
